[project]
name = "foundry_erp"
authors = [
    { name = "Custom", email = "info@example.com"}
]
description = "Custom ERPNext app for foundry business operations"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
erpnext = "~=15.0"
frappe = "~=15.0"
